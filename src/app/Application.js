"use realm";

class Application {
   static main() {
      console.log('1')
         // var packages = ['realm.tags', 'app.tags'];
         // realm.each(packages, function(_p) {
         //    return realm.requirePackage(_p)
         // }).then(function() {
         //    IndexRoute.start();
         // });
   }
}

export Application;
